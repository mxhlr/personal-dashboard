const authConfig = {
  providers: [
    {
      // Clerk Issuer URL from "convex" JWT template
      domain: "assured-bison-80.clerk.accounts.dev",
      applicationID: "convex",
    },
  ],
};

export default authConfig;
